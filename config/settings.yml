default: &default
  wit:
    frontend: <%= ENV['WIT_FRONTEND_TOKEN'] %>
    backend: <%= ENV['WIT_BACKEND_TOKEN'] %>
  tags:
    - rice
    - noodle
    - chinese
    - japanese

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
